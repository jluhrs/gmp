#-----------------------------------------------------------------
# Use this file to add customized Bnd instructions for the bundle
#-----------------------------------------------------------------
Bundle-Activator:
Export-Package: ${bundle.namespace};version="${pom.version}", ${bundle.namespace}.status;version="${pom.version}", ${bundle.namespace}.messagebuilders;version="${pom.version}"
Private-Package: ${bundle.namespace}.*;version="${pom.version}"