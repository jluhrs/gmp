<?xml version="1.0" encoding="utf-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

    <parent>
        <groupId>edu.gemini.aspen.giapi-osgi.build</groupId>
        <artifactId>shared-plugin-settings</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>

    <modelVersion>4.0.0</modelVersion>
    <artifactId>compiled-bundle-settings</artifactId>

    <name>giapi-osgi - bundle instructions</name>

    <packaging>pom</packaging>

    <properties>
        <!-- Matches the date format for the /sbin/date command -->
        <maven.build.timestamp.format>E MMM dd k:mm:ss z yyyy</maven.build.timestamp.format>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>edu.gemini.aspen</groupId>
                <artifactId>giapi</artifactId>
                <version>0.9.1</version>
            </dependency>
            <dependency>
                <groupId>edu.gemini.jms</groupId>
                <artifactId>jms-api</artifactId>
                <version>1.3.0</version>
            </dependency>
            <dependency>
                <groupId>com.google.guava</groupId>
                <artifactId>guava</artifactId>
                <version>8.0.0</version>
            </dependency>
            <dependency>
                <groupId>edu.gemini.epics</groupId>
                <artifactId>epics-service</artifactId>
                <version>0.4.0</version>
            </dependency>
            <dependency>
                <groupId>edu.gemini.aspen.gmp</groupId>
                <artifactId>gmp-commands</artifactId>
                <version>0.9.2-SNAPSHOT</version>
            </dependency>
            <dependency>
                <groupId>org.apache.felix</groupId>
                <artifactId>org.apache.felix.ipojo.annotations</artifactId>
                <version>${org.apache.felix.ipojo}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <type>pom</type>
            <groupId>${project.parent.groupId}</groupId>
            <artifactId>provision</artifactId>
            <version>1.0.0</version>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>org.apache.felix</groupId>
            <artifactId>org.apache.felix.ipojo.annotations</artifactId>
            <version>${org.apache.felix.ipojo}</version>
        </dependency>
        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
            <version>4.7</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>gsbase</groupId>
            <artifactId>gsbase</artifactId>
            <version>2.0.1</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-all</artifactId>
            <version>1.8.5</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.ops4j.pax.exam</groupId>
            <artifactId>pax-exam</artifactId>
            <version>${pax-exam.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.ops4j.pax.exam</groupId>
            <artifactId>pax-exam-junit</artifactId>
            <version>${pax-exam.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.ops4j.pax.exam</groupId>
            <artifactId>pax-exam-junit-extender</artifactId>
            <version>${pax-exam.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.ops4j.pax.exam</groupId>
            <artifactId>pax-exam-junit-extender-impl</artifactId>
            <version>${pax-exam.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.ops4j.pax.exam</groupId>
            <artifactId>pax-exam-container-default</artifactId>
            <version>${pax-exam.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.junit</groupId>
            <artifactId>com.springsource.org.junit</artifactId>
            <version>4.4.0</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <resources>
            <resource>
                <directory>src/main/resources</directory>
            </resource>
        </resources>
        <plugins>
            <plugin>
                <groupId>org.apache.felix</groupId>
                <artifactId>maven-bundle-plugin</artifactId>
                <extensions>true</extensions>
                <configuration>
                    <instructions>
                        <Bundle-SymbolicName>${bundle.symbolicName}</Bundle-SymbolicName>
                        <Bundle-Version>${project.version}</Bundle-Version>
                        <Build-Date>${maven.build.timestamp}</Build-Date>
                        <Build-Host>${maven.build.hostname}</Build-Host>
                        <Bundle-Activator>${bundle.namespace}.osgi.Activator</Bundle-Activator>
                        <Export-Package>${bundle.namespace}.*;version="${project.version}"</Export-Package>
                        <_include>-osgi.bnd, ${basedir}/target/buildmeta.properties</_include>
                        <_debug>true</_debug>
                    </instructions>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-source-plugin</artifactId>
                <version>2.1.2</version>
                <configuration>
                    <attach>true</attach>
                </configuration>
                <executions>
                    <execution>
                        <id>attach-sources</id>
                        <phase>generate-resources</phase>
                        <goals>
                            <goal>jar-no-fork</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.felix</groupId>
                <artifactId>maven-ipojo-plugin</artifactId>
                <version>${org.apache.felix.ipojo}</version>
                <executions>
                    <execution>
                        <goals>
                            <goal>ipojo-bundle</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-antrun-plugin</artifactId>
                <executions>
                    <execution>
                        <id>generate-build-metadata</id>
                        <phase>prepare-package</phase>
                        <goals>
                            <goal>run</goal>
                        </goals>
                        <configuration>
                            <tasks>
                                <taskdef resource="net/sf/antcontrib/antcontrib.properties"
                                         classpathref="maven.plugin.classpath"/>
                                <mkdir dir="${project.build.directory}"/>
                                <exec executable="hostname" osfamily="unix" failifexecutionfails="false"
                                      outputproperty="env.COMPUTERNAME"/>
                                <exec executable="uuidgen" osfamily="unix" failifexecutionfails="false"
                                      outputproperty="build.uuid"/>
                                <property name="hostname" value="${env.COMPUTERNAME}"/>
                                <echo file="${basedir}/target/buildmeta.properties"
                                      message="Build-Host=${hostname}${line.separator}"/>
                                <echo file="${basedir}/target/buildmeta.properties"
                                      message="Build-UUID=${build.uuid}${line.separator}" append="true"/>
                                <condition property="calculateChecksum">
                                    <available file="${basedir}/target/${project.build.finalName}-sources.jar"/>
                                </condition>
                                <if>
                                    <isset property="calculateChecksum"/>
                                    <then>
                                        <checksum file="${basedir}/target/${project.build.finalName}-sources.jar"
                                                  property="src.md5"/>
                                        <echo file="${basedir}/target/buildmeta.properties"
                                              message="Build-MD5=md5: ${project.build.finalName}-sources.jar: ${src.md5}${line.separator}"
                                              append="true"/>
                                    </then>
                                </if>
                            </tasks>
                        </configuration>
                    </execution>
                </executions>
                <dependencies>
                    <dependency>
                        <groupId>ant-contrib</groupId>
                        <artifactId>ant-contrib</artifactId>
                        <version>20020829</version>
                    </dependency>
                </dependencies>
            </plugin>
        </plugins>
    </build>
</project>
