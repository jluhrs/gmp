#-----------------------------------------------------------------
# Use this file to add customized Bnd instructions for the bundle
#-----------------------------------------------------------------

Export-Package: ${bundle.namespace};version="${pom.version}"
Private-Package: ${bundle.namespace}.*;version="${pom.version}"
